import { PricelistHeadingsPipe } from './pricelist-headings.pipe';

describe('PricelistHeadingsPipe', () => {
  it('create an instance', () => {
    const pipe = new PricelistHeadingsPipe();
    expect(pipe).toBeTruthy();
  });
});
